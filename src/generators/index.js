const friendGenerator = require("./friendGenerator");
const groupGenerator = require("./groupGenerator");
const userGenerator = require("./userGenerator");
const transactionGenerator = require("./transactionGenerator");

module.exports = {
  friendGenerator,
  groupGenerator,
  userGenerator,
  transactionGenerator,
};
